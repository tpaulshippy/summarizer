<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Meeting Summaries" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50 text-slate-900">
    <header class="bg-white border-b border-slate-200 fixed top-0 inset-x-0 z-10">
      <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
        <%= link_to "Meeting Summaries", root_path, class: "font-semibold text-xl" %>
        <nav class="flex gap-6 text-slate-600">
          <%= link_to "Meetings", meetings_path, class: "hover:text-slate-900" %>
          <%= link_to "Municipalities", municipalities_path, class: "hover:text-slate-900" %>
        </nav>
      </div>
    </header>
    <main class="max-w-6xl mx-auto px-4 py-24">
      <%= yield %>
    </main>
  </body>
</html>
